{
  "name": "@ichef/get-package-version",
  "private": "true",
  "version": "1.0.0",
  "description": "Read version field from package.json",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "rm -rf ./dist",
    "build": "esbuild src/index.js --bundle --platform=node --target=node24 --format=cjs --outfile=dist/index.js --legal-comments=linked",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@actions/core": "^3.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
