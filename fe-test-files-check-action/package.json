{
  "name": "fe-test-files-check-action",
  "version": "1.0.0",
  "description": "Check if every changed file has corresponding test.",
  "main": "index.js",
  "scripts": {
    "build": "ncc build src/index.js --license LICENSES.txt",
    "clean:build": "rm -rf ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "minimatch": "^6.1.6"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "jest": "^29.4.1"
  }
}
